{
  "name": "app",
  "version": "1.0.0",
  "description": "This project is a **serverless API** built using **Node.js**, deployed with **AWS CDK**, and structured with **AWS API Gateway** routing requests to **AWS Lambda** functions.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-cdk/aws-apigateway": "^1.203.0",
    "@aws-cdk/aws-lambda": "^1.203.0",
    "@aws-cdk/core": "^1.203.0",
    "aws-sdk": "^2.1692.0"
  }
}
